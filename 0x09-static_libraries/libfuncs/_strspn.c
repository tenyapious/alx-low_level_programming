#include <stdio.h>

/**
 * _strspn - Computes the length of the initial segment of string 's'
 *           which consists entirely of characters from string 'accept'.
 * @s: The string to be searched.
 * @accept: The string containing the characters to be accepted.
 *
 * Return: The number of characters in the initial segment of 's'
 * that match 'accept'.
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
